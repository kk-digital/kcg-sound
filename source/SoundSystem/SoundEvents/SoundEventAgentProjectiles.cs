using System;
using Godot;
using System.Collections.Generic;

namespace SoundEvent;
public class SoundEventAgentProjectiles
{
    /*

    public void ShootFire(string weapon, int entityId, RayCast2D raycast)
    {
        public RayCast2D raycast = new RayCast2D(); // Must be passed and this deleted

        if (raycast.IsColliding())
        {
            public Object collider = raycast.GetCollider();
            SoundEventTools.SpecifyMaterial(weapon, collider.materialId); // Play sound of impact of material surface
        }
        else
        {
            SoundApi.playbackManager.playSound(weapon, entityId);  // Play sound of impact of material surface
        }
    }

    */
}
